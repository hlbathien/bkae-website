// Fonts loaded via Google Fonts @import in globals.css (Syne, Instrument Serif, DM Mono).
// Centralized class helpers if needed.
export const fontClass = {
  display: "font-display",
  serifItalic: "font-serif-italic",
  mono: "font-[var(--font-mono)]",
} as const;
