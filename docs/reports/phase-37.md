# Phase 37 — cms-live-smoke-normalization

## Scope
Bring up real Postgres, generate types + migration, apply, seed, smoke prod build.
Surface + fix Post.category / Post.cover / Project.cover / Project.links shape mismatches exposed by live DB.

## Files
- src/lib/cms-server.ts (normalizePost, RawMedia, mediaUrl, normalizeProject extended for cover + links)
- src/app/(marketing)/journal/tag/[slug]/page.tsx (null-safe category)
- src/migrations/20260423_143307_stats_process_globals.ts (+json) (generated)
- src/migrations/index.ts (autogenerated list)
- src/payload-types.ts (regenerated)

## Acceptance
- docker compose up: ✅ bkae-db healthy
- generate:types: ✅ (via node_modules/.bin/tsx direct invocation — pnpm script blocked by Node 23 + payload CJS loader; scripts untouched)
- migrate:create: ✅ (stats_process_globals.ts)
- migrate apply: applied via direct psql + payload_migrations insert (`pnpm migrate` CLI hangs on first-run prompt under Node 23 / Windows TTY; symptom only, not schema drift)
- seed: ✅ (idempotent; stats_board + process_flow + home_page populated)
- pnpm build: ✅ live DB round-trip clean
- smoke landing: "The receipts", "Two systems", "Hackathon placings", "Teams beaten", "contract at every" all rendered from Payload
- /projects/lumen-journal: 8 JSON-LD script blocks (org + website + creativeWork + breadcrumb series)
- /llms.txt: live Payload data
- /sitemap.xml: live
- /api/rss: live
- /admin: HTTP 200

## Notes
- Post.category relation (Tag) → string name in normalizePost; null/undefined handled.
- Post.cover and Project.cover upload → url via mediaUrl, falls back to static /journal-1.jpg / /cover-lumen.jpg placeholders.
- Project.links Payload array (kind-tagged) → { github, demo } object to preserve mock Project.links contract.
- tsx/esm loader incompat pre-existing — worked around by invoking `node_modules/.bin/tsx node_modules/payload/bin.js` directly; no config change to avoid scope creep.
